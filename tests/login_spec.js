const userData = require('../data/userData');
const homePage = require('../page-objects/homePage');

it('Login test credentials', function () {

    // homePage.disableAngular();
    /* homePage.getLoginPage();
    homePage.setName(userData.user.username);
    homePage.setPassword(userData.user.password);
    homePage.getUsername();
    homePage.getPassword();

    expect(homePage.getUsername()).toEqual(userData.user.username);
    expect(homePage.getPassword()).toEqual(userData.user.password);

    homePage.login();

    expect(browser.get('https://cuddleclones.com/my-account/')); */

});

